{
  "name": "cloud-learning-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "pnpm -r install",
    "dev": "pnpm -C server start & pnpm -C frontend dev",
    "start-server": "pnpm -C server start",
    "start-web": "pnpm -C frontend dev"
  }
}
